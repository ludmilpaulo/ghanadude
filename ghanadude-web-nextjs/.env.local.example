# Set this to your deployed Django API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: If images are served from another host (e.g. /media/)
NEXT_PUBLIC_IMAGE_BASE_URL=http://localhost:8000
